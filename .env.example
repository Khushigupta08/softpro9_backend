PORT=5000
ADMIN_JWT_SECRET=your_admin_jwt_secret_here
DB_FILE=./softpro9.db

# If you want to create admin accounts via the registration route, set a creation key
# Use the same value in requests to `POST /api/auth/register` as `adminKey` when role='admin'
ADMIN_CREATE_KEY=your_admin_create_key_here

# Example: Set allowed origins if you want to lock down CORS
# ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend.vercel.app
